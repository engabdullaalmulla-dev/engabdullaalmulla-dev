{
  "name": "marble-nations-app",
  "version": "0.1.0",
  "private": true,
  "description": "iOS shell for the Marble Nations prototype.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-www.mjs",
    "sync": "npm run build && npx cap sync ios",
    "ios": "npm run sync && npx cap open ios",
    "add:ios": "npx cap add ios"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
